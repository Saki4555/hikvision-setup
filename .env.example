# =============================================
#  HIKVISION ATTENDANCE SYSTEM - CONFIG FILE
# =============================================

# --- MySQL (XAMPP) Settings ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=attendance_db

# --- Polling interval (how often to check devices, in milliseconds) ---
# 10000 = 10 seconds
POLL_INTERVAL_MS=10000